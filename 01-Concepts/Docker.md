**容器确实是用本机的 Linux 内核在运行进程**。但关键的区别在于：

- **宿主机环境** = 直接使用你电脑的真实文件系统 + 你安装的软件
    
- **容器环境** = 仍然使用同一台电脑的 Linux 内核，但**看到的是一个独立的、由镜像和映射组成的文件系统**
    
目录映射就是把你自己电脑上的一个文件夹，**临时性地替换掉容器里原本由镜像提供的某个文件夹**，让容器既能保持环境的独立性，又能实时获取你正在开发的代码。