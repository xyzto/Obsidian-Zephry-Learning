# Handoff — 对话交接文件

> 用途：上下文接近上限时由 AI 写入，供下一个对话的 AI 读取，实现无缝接续。
> 新对话开始时，若此文件有内容，优先读它，再读 session-log.md。
> 交接完成、确认新 AI 已定位后，可清空"## 交接状态"以下内容（保留模板结构）。

---

## 交接状态

**写入时间**：2026-03-20
**触发原因**：🔴 红色警告——本次对话读取文件数量多、轮次长

### 本次对话改动的文件

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `_manual/vault-prompt.md` | 更新 | 新增上下文管理机制（两级警告 + handoff 流程）；移除所有硬编码绝对路径，改为读 vault-root.md 拼接 |
| `_manual/handoff.md` | 新建→更新 | 本文件，对话交接模板 |
| `_manual/session-log.md` | 更新 | 记录本次操作，更新启动流程 |
| `_manual/vault-root.md` | 新建 | 根路径单一真相源，`E:\OB-Vaults\Zephry` |
| `_manual/new-project-guide.md` | 更新 | 移除硬编码路径，引用 vault-root.md |
| `_manual/structure.md` | 更新 | 新增 vault-root.md、handoff.md 条目 |

### QEMU 实验进度

- 已完成：01–05
- **下一个：06 定时器**

### 未完成的任务

无。本次对话全部为 `_manual/` 系统文件优化，无实验落地任务。

### 需要注意的上下文

- `_manual/ai/` 子文件夹的分类方案**讨论过但未执行**——用户确认了"只建 ai/ 子文件夹"的方向，但因路径重构工作量大，本次优先做了路径软化，子文件夹分类留待下次。
- vault-prompt 里的"唯一绝对路径入口"说明：新对话粘贴提示词时，用户需要确保 MCP 能访问到 `_manual\vault-root.md`，AI 从那里读根路径，后续全部自行拼接。

### 给下一个 AI 的指令

读完本文件后，告诉用户：
"已从 handoff.md 定位。本次系统优化全部完成，QEMU 进度 01-05，下一个是 06 定时器。还有一个待议项：`_manual/ai/` 子文件夹分类尚未执行，需要时可以继续。等你的指令。"
